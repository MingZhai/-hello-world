#include "priority_queue.h"

int main(void){
	El_Iterator * self;
	bool isAtEnd;
	isAtEnd = El_Iterator_isAtEnd ( El_Iterator * self );
	if(isAtEnd == false){printf("Iterator is not at end%s\n");}
	return 0;
}
